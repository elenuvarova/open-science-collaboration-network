node_modules
**/node_modules
dist
build
# Never bake secrets into the image — exclude env files at any depth.
.env
.env.local
.env.*
**/.env
**/.env.*
.git
*.log
.DS_Store
# Python / local artifacts that must not leak into the image.
**/.venv
**/venv
**/__pycache__
**/*.py[cod]
*.sqlite
*.sqlite-journal
# The ETL is now bundled into the image, but its local-dev artifacts must not be:
# the venv (huge, platform-specific) and the downloaded CORDIS ZIPs (regenerated).
etl/.venv
etl/.cache
backend/public
